#!/usr/bin/env bash
set -e
CONTAINER_NAME=rrq_redis
if [ "$1" = "start" ]; then
    docker run --rm -d --name=$CONTAINER_NAME -p 127.0.0.1:6379:6379 redis
elif [ "$1" = "stop" ]; then
    docker stop $CONTAINER_NAME
else
    echo "Usage: redis <start|stop>"
    exit 1
fi
